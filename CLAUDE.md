# CLAUDE.md

This project's conventions live in [`AGENTS.md`](AGENTS.md). Read that file as if it were addressed to you. aegis maintains a single source of truth for agent instructions; do not let `AGENTS.md` and `CLAUDE.md` drift.

**Convention**: when `AGENTS.md` changes, this file may need a corresponding update only if the change affects how Claude Code (specifically) reads or applies the conventions. Otherwise, this file stays a thin pointer.
